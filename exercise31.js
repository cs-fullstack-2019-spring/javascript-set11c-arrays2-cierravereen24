// Creating array w/ 5 numbers,
// prompting the user to pick and index number,
// then alerting the user of the index.

var numbers = [3, 6, 12, 18, 24];
var index = parseInt(prompt("Pick a number 0-4 to pick an index."));

alert("The index you chose is: " + numbers[index]);

console.log(numbers[index]);